<?xml version="1.0" encoding="utf-8"?>
<template>

	<t t-name="bi_pos_product_template.producttemplatepopup" owl="1">
		
		 <div class="popup popup-confirm">
            <div class="modal-header">
                <p style="color: #714b67;font-weight: bold;font-size: 20px; margin-left:155px;">Products Variants</p>
            </div>

            <main class="body">
                <div style="overflow : auto">
					<div t-if="props.variant_ids.length != 0" class="product-list {{this.pos.productListViewMode}} p-1" style="max-height: 360px;overflow: auto; display: grid;">
						<t t-foreach="props.variant_ids" t-as="product" t-key="product.id">
							
							<t t-set="count" t-value="pos.db.product_by_id[product.id]"/>
							<article tabindex="0" class="flex-column product position-relative btn btn-light d-flex align-items-stretch p-0 m-0 text-start cursor-pointer overflow-hidden transition-base " t-on-click="() => this.add_product_variant(product)">
						            <div class="product-img" >
						                <img t-att-src="count.getImageUrl()" t-att-alt="count.display_name"  />
						            </div>
						            <div class="product-content d-flex flex-column justify-content-between h-100 mx-2 py-1">
						                <div class="product-name" style="font-size: 14px;display: flex" t-attf-id="article_product_{{product.id}}">
						                    <t t-esc="count.display_name" /><br/>
						                    <t t-esc="env.utils.formatCurrency(count.lst_price)" />

						                </div>
					              
						            </div>
					        </article>
					  

						</t>
					</div>
					<t t-if="env.services.pos.config.allow_alternative_products">
						<t t-if="props.alternative_prod.length != 0">
							<p style="color: #714b67;font-weight: bold;font-size: 20px;">
								Select Your Alternative Products
							</p>
							<div t-if="props.variant_ids.length != 0" class="product-list {{this.pos.productListViewMode}} p-1" style="max-height: 270px;overflow: auto; display: grid;">
								<t t-foreach="props.alternative_prod" t-as="product" t-key="product.id">
									<t t-set="count" t-value="pos.db.product_by_id[product.id]"/>
									<article tabindex="0" class="flex-column product position-relative btn btn-light d-flex align-items-stretch p-0 m-0 text-start cursor-pointer overflow-hidden transition-base " t-on-click="() => this.add_product_variant(product)">
							            <div class="product-img">
							                <img t-att-src="product.getImageUrl()" t-att-alt="product.display_name" />
							            </div>
							            <div class="product-content d-flex flex-column justify-content-between h-100 mx-2 py-1" >
							                <div class="product-name" style="font-size: 14px;display: flex" t-attf-id="article_product_{{product.id}}">
							                    <t t-esc="product.display_name" />
							                </div>
							                <span class="price-tag">
							                    <t t-esc="env.utils.formatCurrency(product.lst_price)" />
							                </span>
							            </div>
							        </article>
								</t>
							</div>
						</t>
					</t>
				</div>
            </main>

            <footer class="footer modal-footer">
                <div class="button confirm highlight btn btn-lg btn-primary" t-on-click="confirm">
                    <t t-esc="props.confirmText" />
                </div>
               

            </footer>
        </div>
			
	</t>

	<t t-name="ProductTemplate" t-inherit="point_of_sale.ProductCard" t-inherit-mode="extension" owl="1">

		<xpath expr="//div[hasclass('product-content')]" position="replace">

			 <div class="product-content d-flex flex-column justify-content-between h-100 mx-2 py-1">
			 	<t t-set="count" t-value="pos.db.product_by_id[props.productId]"/>
			 	
				 	<t t-if="env.services.pos.config.allow_product_variants">
				 		<t t-if="count.product_variant_count > 1">
			                <div class="overflow-hidden lh-sm fw-bolder product-name"
			                    t-att-class="{'no-image': !props.imageUrl}" 
			                    t-attf-id="article_product_{{props.productId}}"
			                    t-esc="count.name" />
			            </t>
			            <t t-else="">
							<t t-esc="props.name" />
						</t>
			                <span t-if="props.price" class="price-tag text-primary py-1 fw-bolder" />
			      
			                <t t-if="count.product_variant_count > 1">
								<t t-esc="count.product_variant_count"/> Variants
							</t>
							<t t-else="">
								<t t-esc="props.price" />
							</t>
		            </t>
		            <t t-else="">
		            	<div class="product-content d-flex flex-column justify-content-between h-100 mx-2 py-1">
		                <div class="overflow-hidden lh-sm fw-bolder product-name"
		                    t-att-class="{'no-image': !props.imageUrl}" 
		                    t-attf-id="article_product_{{props.productId}}"
		                    t-esc="props.name" />
		                <span t-if="props.price" class="price-tag text-primary py-1 fw-bolder"
		                    t-esc="props.price" />
	            		</div>
	            	</t>
            </div>
        </xpath>
    </t>
			
	<t t-name="ProductTemplateListWidget" owl="1">
		<div class='productt-list-container'>
			<div class="productt-list-scroller touch-scrollable  scrollable-y">
				<div class="productt-list">
				</div>
			</div>
			<span class="placeholder-ScrollbarWidget" />
		</div>
	</t>

	

</template>
