<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="product_purchase_template_form_view" model="ir.ui.view">
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_only_form_view" />
        <field name="arch" type="xml">
            <xpath expr="//page[@name='sales']" position="after">
                <page name="purchaselist" string="Purchase List">
                    <field name="purchase_order_product_template_line_ids"
                        domain="[('product_template_id','=',self.id)]">
                        <tree string="Purchase Order Lines" create="false">
                            <field name="date_order" optional="show"/>
                            <field name="partner_id" string="Supplier" optional="show"/>
                            <field name="order_id" optional="show"/>
                            <field name="product_uom_qty" optional="show"/>
                            <field name="qty_received" optional="show"/>
                            <field name="qty_invoiced" optional="show"/>
                            <field name="price_unit" optional="show"/>
                            <field name="price_subtotal" optional="show"/>
                            <field name="price_tax" optional="show"/>
                            <field name="price_total" optional="show"/>
                            <field name="currency_id" optional="show"/>
                        </tree>
                    </field>
                </page>
            </xpath>
        </field>
    </record>
</odoo>